name: Nanit Daemon
description: Go backend daemon for the Nanit baby camera integration. Provides sensor data, camera controls, and HLS streaming.
version: 0.1.0
slug: nanitd
url: https://github.com/wealthystudent/ha-nanit
arch:
  - aarch64
  - amd64
  - armv7
  - armhf
  - i386
init: false
startup: application
boot: auto
ports:
  8080/tcp: null
ports_description:
  8080/tcp: nanitd HTTP API (internal only)
map:
  - addon_config:rw
  - ssl:ro
options:
  log_level: info
schema:
  log_level: list(trace|debug|info|warning|error)?
image: ghcr.io/wealthystudent/ha-nanit-nanitd-{arch}
